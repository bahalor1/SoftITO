@model KütüphaneUygulaması.Models.Kategori

@{
    ViewData["Title"] = "Delete";
}

<div class="alert alert-danger mt-4">
    <h3> Uyarı: Bu kategoriyi silmek istediğinize emin misiniz?</h3>
    <p class="mb-0">Bu işlemi onaylarsanız kategori veritabanından kalıcı olarak silinecektir.</p>
</div>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.KategoriAdi)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.KategoriAdi)
        </dd>
    </dl>
   
    <form asp-action="Delete" class="mt-4">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger fw-bold"> Evet, Kalıcı Olarak Sil</button>
        <a asp-action="Index" class="btn btn-secondary ms-2">İptal ve Geri Dön</a>
    </form>
</div>
